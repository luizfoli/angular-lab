import { TooltipDirectiveDirective } from './tooltip.directive';

describe('TooltipDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
